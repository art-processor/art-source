
   Heap?@elmtType?BufferR(int cap, int lim) {		// package-private

	super(cap, lim);
	this.isReadOnly = true;

    }

    Heap?@elmtType?BufferR(?@elmttype?[] buf, int off, int len) { // package-private

	super(buf, off, len);
	this.isReadOnly = true;

    }

    protected Heap?@elmtType?BufferR(?@elmttype?[] buf,
				   int mark, int pos, int lim, int cap,
				   int off)
    {

	super(buf, mark, pos, lim, cap, off);
	this.isReadOnly = true;

    }
